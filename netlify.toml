# Netlify build config for the Sphinx docs site — connect the repo in the Netlify dashboard to deploy

[build]
command = "pip install uv && python3 -m uv sync && python3 -m uv run poe docs"
publish = "docs/_build/html"

[build.environment]
# Picks up mise.netlify.toml instead of .tool-versions, so mise installs only the pinned interpreter
# here rather than the full 3.8-3.14 matrix .tool-versions lists for local tox runs
MISE_ENV = "netlify"

# Persists uv's package cache and mise's installed Python 3.14 across builds — without this every
# build re-downloads dependencies and re-installs (sometimes source-compiles) the interpreter
[[plugins]]
package = "netlify-plugin-cache"
[plugins.inputs]
paths = ["~/.cache/uv", "~/.local/share/mise/installs/python/3.14"]

# Force the default netlify.app subdomain to the custom domain — avoids duplicate-content SEO
[[redirects]]
from = "https://pipef.netlify.app/*"
to = "https://pipef.aktolentino.com/:splat"
status = 301
force = true
